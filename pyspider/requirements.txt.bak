Flask>=0.10 --web 框架
Jinja2>=2.7 --模版引擎
chardet>=2.2 --用来实现字符串/文件编码检测模板
cssselect>=0.9 --cssselect parses CSS3 Selectors and translates them to XPath 1.0
lxml --xml 处理
pycurl --cURL library module for Python
pyquery --jquery
requests>=2.2 --Requests is an Apache2 Licensed HTTP library, written in Python, for human being
tornado>=3.2 --Tornado is a Python web framework and asynchronous networking library, originally developed at FriendFeed
mysql-connector-python>=1.2.2 
pika>=0.9.14 --Pika Python AMQP Client Library
pymongo>=2.7.2,<3.0 --Python driver for MongoDB
unittest2>=0.5.1 --The new features in unittest backported to Python 2.4+
Flask-Login>=0.2.11 --User session management for Flask
u-msgpack-python>=1.6 --A portable, lightweight MessagePack serializer and deserializer written in pure Python.
click>=3.3 --A simple wrapper around optparse for powerful command line utilities.
SQLAlchemy>=0.9.7 -- jdbc
six --Python 2 and 3 compatibility utilities
amqp>=1.3.0 --Low-level AMQP client for Python (fork of amqplib)
redis --Python client for Redis key-value store
kombu --Messaging library for Python
